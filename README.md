# Base FastAPI Application

### Description
Minimal FastAPI application (template) with rate limiter on redis, database api and updates system.

## How to start
write `docker-compose up --build` to shell
